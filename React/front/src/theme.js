import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#d32f2f',
      light: '#d32f2f',
      dark: '#d32f2f',
    },
    info: {
        main: '#d32f2f',
        light: '#d32f2f',
        dark: '#d32f2f',
      },
  },
});

export default theme;